import React from 'react';
import { useAuth0 } from '@auth0/auth0-react';
import { Container, Box, Button, Typography } from '@mui/material';

export function StatsPage() {

  return (  
    <div>
      <img src="stats.png" alt="stats" width={380} />
    </div>
  );
}
